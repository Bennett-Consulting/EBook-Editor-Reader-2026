# Frontend environment variables
# Copy this to .env and fill in your values

# No backend required! AI provider keys are managed in-app via Settings → AI Providers.
# The app calls AI providers directly from the device using the user's own API key.
#
# Legacy: If you still want to use the backend proxy (server.py), uncomment:
# EXPO_PUBLIC_BACKEND_URL=http://localhost:8000
